---
description: 心理戰輸入與紀錄表 v1 已訂版，精煉時不得改欄位契約
alwaysApply: true
---

# 心理戰 v1（已訂版）

`#page-psych[data-psych="v1"]` 契約：

- 頂部共用：日期、時間（可打字）、時段（開盤前／收盤後）
- 三格輸入並排：心理紀錄、行為記錄、事件
- 心理：能量、心情、狀態（1–5）、狀態標籤（可多選）、一句話
- 行為：行為模型（點選 ±）、一句話。無策略。
- 事件：事件類型（點選 ±）、一句話。無影響（利多／利空）。
- 下方總表欄位由左到右：時間、時段、項目、狀態、備註、操作
- 狀態欄：心理＝能量／心情／狀態（可附標籤）；行為＝行為模型；事件＝事件類型
- 點表頭排序。紀錄不進入損益。

精煉只允許：版面、快捷、DOM 少重繪、輸入正規化。不得增刪欄位或改 SSOT。
